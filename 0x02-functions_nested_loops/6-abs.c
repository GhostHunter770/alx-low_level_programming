#include "main.h"
/** 
 * _abs = computes the absolute calue of an integer
 *
 *@n: the integer to check
 *
 * Return:absolute value
 */
int _abs(int n)
{
	if (n <= 0)
	{
		return(n);
	}
	else
	{
		return (-n);
	}
}
