#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: integer one
 * @b: integer two
 *
 * Return: thhe result of multiplying a and b.
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
